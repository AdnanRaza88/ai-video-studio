import 'package:flutter/foundation.dart';

class SessionService extends ChangeNotifier {
  // Placeholder for session + message management.
  // Will use SQLite + context manager as described in the architecture.
}
